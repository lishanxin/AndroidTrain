<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="WelcomeTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/test_background</item>
        <!--<item name="android:windowFullscreen">true</item>-->
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
    </style>

    <!-- your theme inherits from the material theme -->
    <style name="MaterialMainTheme" parent="android:Theme.Material.Light">
        <item name="android:colorPrimaryDark">@color/white</item>
        <!--windowLightStatusBar为true时，statusBarText颜色为黑色，否则为白色-->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:textColorPrimary">@color/green</item>
        <item name="android:colorPrimary">@color/orange</item>
        <item name="android:windowBackground">@color/paleBlue</item>
        <item name="android:navigationBarColor">@color/brown</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">@color/black</item>
        <item name="android:colorControlHighlight">@color/red</item>
        <item name="android:colorButtonNormal">@color/teal</item>

        <!--enable window content transitions-->
        <item name="android:windowContentTransitions">true</item>

        <!--specify enter and exit transitions-->
        <item name="android:windowEnterTransition">@transition/explode</item>
        <item name="android:windowExitTransition">@transition/explode</item>

        <!--specify shared element transitions-->
        <item name="android:windowSharedElementEnterTransition">@transition/change_image_transform</item>
        <item name="android:windowSharedElementExitTransition">@transition/change_image_transform</item>
        <!-- theme customizations -->

    </style>
    <style name="MaterialBaseTheme" parent="android:Theme.Material">
        <item name="android:colorPrimaryDark">@color/pink054</item>
        <item name="android:colorPrimary">@color/pink</item>
        <!--<item name="android:windowBackground">@color/yellow</item>-->
    </style>
</resources>